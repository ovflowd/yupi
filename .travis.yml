language: csharp
solution: Yupi.sln

install:
  - nuget # lets get version number
  - nuget restore Yupi.sln -Verbosity detailed
  - nuget update Yupi.sln -Verbosity detailed
before_install:
  - pip install --user codecov
after_success:
  - codecov
mono:
  - latest
